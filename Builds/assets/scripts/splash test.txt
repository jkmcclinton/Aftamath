Script Splash
lockplayer
declare(c, global, Integer, 0)
random(c, 1, 3)

if([c = 1] )
	splash(fight)
else
	if([c = 2])
		splash(kick_ass)
	else
		splash(kill_it)
	end
end
	
wait(3)

setSpeaker(this)
text({...Yeah, sorry m8.})
text({Fighting wasn't included in this version.})
done


