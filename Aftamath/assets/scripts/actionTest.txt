Script Main
lockplayer

declare(part, local, integer, 0)
if([part = 0])
	value(set, part, 1)
	doAction(this, knockout, null, on_level_change)
else
	doAction(this, recover)
	value(set, part, 0)
end

done
